import {ScrollView, Text, StatusBar} from 'react-native'

export default function Register(){

  return(
    <ScrollView>
      <StatusBar />
      <Text>
        Register Page
      </Text>
    </ScrollView>
  );
}